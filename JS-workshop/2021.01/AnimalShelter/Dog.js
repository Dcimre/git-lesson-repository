import Animal from "./Animal.js";


 class Dog extends Animal{

    healCost = Math.floor(Math.random() * 8 + 1);
    
  /* constructor(name,ownerName,stateOfHealth) {
        super.name = name;
        super.ownerName = ownerName;
        super.stateOfHealth = stateOfHealth
        
    }
    

*/

}

export default Dog;